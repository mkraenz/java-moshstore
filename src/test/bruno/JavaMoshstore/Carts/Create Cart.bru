meta {
  name: Create Cart
  type: http
  seq: 1
}

post {
  url: {{url}}/carts
  body: none
  auth: inherit
}

vars:post-response {
  cartId: res('id')
}

settings {
  encodeUrl: true
  timeout: 0
}
