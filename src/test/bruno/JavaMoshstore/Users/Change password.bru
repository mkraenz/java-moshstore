meta {
  name: Change password
  type: http
  seq: 1
}

post {
  url: {{url}}/users/{{userId}}/change-password
  body: json
  auth: inherit
}

params:query {
  : 
}

body:json {
  {}
}

settings {
  encodeUrl: true
  timeout: 0
}
