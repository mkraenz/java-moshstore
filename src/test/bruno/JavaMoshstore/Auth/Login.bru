meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{url}}/auth/login
  body: json
  auth: inherit
}

headers {
  content-type: application/json
}

body:json {
  {
    "email": "1124-1405@example.com",
    "password": "pw1234"
  }
}

vars:post-response {
  accessToken: res('accessToken')
}

settings {
  encodeUrl: false
  timeout: 0
}
