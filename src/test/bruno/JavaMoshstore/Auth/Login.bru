meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{url}}/auth/login
  body: json
  auth: inherit
}

headers {
  content-type: application/json
}

body:json {
  {
    "email": "{{email}}",
    "password": "pw1234"
  }
}

vars:post-response {
  accessToken: res('accessToken')
}

settings {
  encodeUrl: false
  timeout: 0
}
