meta {
  name: Register User
  type: http
  seq: 1
}

post {
  url: {{url}}/users
  body: json
  auth: inherit
}

headers {
  content-type: application/json
}

body:json {
  {
    "email": "{{email}}",
    "name": "asd",
    "password": "pw1234"
  }
}

script:post-response {
  bru.setVar('userId', res.body.id);
}

settings {
  encodeUrl: false
  timeout: 0
}
