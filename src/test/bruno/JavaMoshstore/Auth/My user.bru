meta {
  name: My user
  type: http
  seq: 2
}

get {
  url: {{url}}/auth/me
  body: none
  auth: inherit
}

script:post-response {
  bru.setVar('userId', res.body.id);
}

settings {
  encodeUrl: false
  timeout: 0
}
